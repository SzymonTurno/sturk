#ifndef UNITTEST_H
#define UNITTEST_H

#include "unity.h"
#include "unity_fixture.h"
#include "UB/logger/log.h"

#define ARRAY_SIZE(array) (sizeof(array) / sizeof(array[0]))

#define LINE(str) str"\n",

#endif /* UNITTEST_H */
